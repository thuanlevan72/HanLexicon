version: '3.8'

services:
  mandarin_flow:
    build: .
    container_name: mandarin_flow_app
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
